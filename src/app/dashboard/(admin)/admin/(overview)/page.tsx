import AdminPage from "~/app/dashboard/(admin)/admin/(overview)/client";

export const metadata = {
  title: "Dashboard",
  description:
    "Super Admin Dashboard using lol UI components, ehen, Wo, wetin concine me? adhering to design specifications and best practices for accessibility.",
};

const Admin = () => {
  return <AdminPage />;
};

export default Admin;
